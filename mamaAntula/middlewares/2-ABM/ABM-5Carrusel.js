"use strict";
// errores sólo para el guardado

export default async (req, res, next) => {
	// Variables
	const {tema_id} = req.body;
	mensajes = [];

	// Validaciones
	if (false) {
	}

	// Fin
	return next();
};
