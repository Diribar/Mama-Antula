<!-- Encabezado -->
<section id="filtros" class="flexRowAlign">
	<select name="seccion_id">
		<% for (const seccion of secciones) { %>
			<option value="<%= seccion.id %>"><%= seccion.nombre %></option>
		<% } %>
	</select>
	<select name="tema_id" class="ocultar"></select>
	<select name="pestana_id" class="ocultar"></select>
	<select name="encabezado" class="ocultar"></select>
</section>
<section id="encabezados">
	<div id="encabSinIndice" class="encabezado ocultar">
		<input type="text" name="titulo">
		<input type="text" name="subTitulo">
		<select name="orden">

		</select>
	</div>
	<div id="encabCartas" class="encabezado ocultar">
		<input type="number" name="numero">
		<select name="nombreDesde_id">

		</select>
		<select name="nombreHacia_id">

		</select>
		<select name="lugar_id">

		</select>
		<select name="idioma_id">

		</select>

		<div id="areaSoltar">
			<p id="cartelImagen">Arrastrá o buscá tu imagen (opcional)</p>
			<input name="imagen" type="file" accept="image/*" hidden><!-- el input no lleva class 'input', porque tiene un tratamiento diferente -->
			<img id="vistaImagen" src="">
		</div>

		<input type="date" name="fechaEvento">
	</div>
	<div id="encabExpers" class="encabezado ocultar">
		<input type="text" name="titulo">
		<input type="text" name="subTitulo">
		<select name="lugar_id">

		</select>
		<input type="date" name="fechaEvento">
	</div>
	<div id="iconosEncab">
		<!-- guardar -->
		<!-- eliminar -->
	</div>
</section>

<!-- Contenido -->
<section id="contenidoActual" class="ocultar">
	<div class="bloque">
		<div></div>
		<div id="iconosContActual">
			<!-- guardar -->
			<!-- eliminar -->
			<!-- subir -->
			<!-- bajar -->
		</div>
	</div>
</section>
<section id="contenidoNuevo" class="ocultar">
	<div>
	</div>
	<!-- guardar -->
</section>
