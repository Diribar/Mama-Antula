<div id="encabezado" class="<%= encabezado.statusRegistro.codigo %>" data-encab_id="<%= encabezado.id %>">
	<% if (cambioStatus) { %>
		<!-- Íconos de aprueba / rechaza -->
		<%- include("REV-9ApruebaRech") %>
	<% } %>

	<!-- Ruta -->
	<h2><%= ruta %></h2>

	<% if (cambioStatus) { %>
		<!-- Imagen del usuario que sugirió el status -->
		<%- include("REV-9ImgUsuario", {imagenUsuario: encabezado.imagenUsuario, nombreCompleto: encabezado.usuario.nombreCompleto}) %>
	<% } %>

</div>
<% if (encabezado.tituloElab || encabezado.titulo) { %>
	<h3><%= encabezado.tituloElab || encabezado.titulo %></h3>
<% } %>
