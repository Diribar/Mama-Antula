<section id="menuTituloTema" class="flexRowAlign">
	<%- include("MN-1Temas") %>

	<% tema = temasSeccion[0] %>
	<h1 id="tituloTema"><%= tema.titulo %></h1>
</section>

<section id="contenidoTemas">
	<% for (const tema of temasSeccion) { %>
		<!-- Tema -->
		<div class="contenidoTema contenido <% if (tema.id != temaActual.id) { %>ocultar<% } %>" data-tema_id="<%= tema.id %>">
			<!-- Sin pestañas -->
			<% if (!tema.pestanas.length) { %>
				<%- include("MN-3ArticCarta", {tema_id: tema.id, sonCartas: tema.codigo == "cartas"}) %>

			<% } else { %>
				<!-- Tiene pestañas - Pestañas -->
				<%- include("MN-2Pestanas", {tema}) %>

				<!-- Tiene pestañas - Contenido -->
				<% for (const pestana of tema.pestanas) { %>
					<% console.log(21, pestana) %>
					<div class="contenidoPestana contenido <% if (!pestanaActual || pestana.id != pestanaActual.id) { %>ocultar<% } %>" data-pestana_id="<%= pestana.id %>">
						<%- include("MN-3ArticCarta", {pestana}) %>
					</div>
				<% } %>
			<% } %>
		</div>
	<% } %>
</section>
